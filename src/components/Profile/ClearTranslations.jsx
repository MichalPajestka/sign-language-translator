import React from "react";

const ClearTranslations = ({ clearTranslations }) => {
	return (
		<div>
			<button onClick={clearTranslations}>Clear Translations</button>
		</div>
	);
};

export default ClearTranslations;
